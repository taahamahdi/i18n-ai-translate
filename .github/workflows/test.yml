name: test

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Test i18n-ai-translate action
        uses: taahamahdi/i18n-ai-translate@60935f1219ea401d63ad081fcd6a2b67cd4be365
        with:
          json-file-path: test/en.json
          api-key: ${{ secrets.OPENAI_API_KEY }}
